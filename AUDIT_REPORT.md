# Звіт про аудит безпеки та логічної цілісності

**Дата:** 2025-12-11
**Виконавець:** Sentinel 🛡️ - Агент з безпеки та цілісності
**Статус:** ЗАВЕРШЕНО

## I. Вступ

Цей звіт представляє результати комплексного аудиту безпеки та функціональної цілісності чотирьох ключових модулів екосистеми "Dynamic SkyBlock LXD System". Аудит проводився відповідно до директиви від 2025-12-11 з метою виявлення вразливостей, логічних помилок та архітектурних недоліків без внесення змін до кодової бази.

## II. Зведення ключових знахідок

Аудит виявив низку критичних проблем у всіх модулях. Найбільш серйозні з них включають:

1.  **Критичні вразливості безпеки:** Відсутність автентифікації та авторизації в API, що дозволяє будь-якому користувачеві виконувати дії від імені іншого. Відсутність перевірок прав доступу до команд у проксі-плагіні.
2.  **Порушення логічної цілісності:** Численні race conditions, особливо в логіці підключення гравців та синхронізації даних, що може призводити до некоректної роботи квестів, серверів та автоматичного заморожування островів.
3.  **Архітектурні проблеми:** Надмірно жорсткий зв'язок (tight coupling) між модулями, неефективні (блокуючі) мережеві запити на головному потоці сервера та використання механізму опитування (polling) замість подій, що створює перешкоди для масштабування та підтримки системи.

Детальний аналіз наведено нижче та згруповано за трьома обов'язковими категоріями.

---

## 🚨 Категорія 1: Критичні вразливості безпеки

### Модуль: API

1.  **Небезпечне зберігання облікових даних (Hardcoded Secrets):**
    *   **Проблема:** Файл `api/app/core/config.py` містить жорстко закодований `DATABASE_URL` з повними обліковими даними (`skyblock_user:skyblock_pass`).
    *   **Ризик:** Якщо змінна середовища `DATABASE_URL` не буде встановлена, додаток використає ці облікові дані, що призведе до несанкціонованого доступу до бази даних.

2.  **Відсутність авторизації в ключових ендпоінтах:**
    *   **Проблема:** Ендпоінти в `api/app/api/v1/endpoints/teams.py` (перейменування команди, прийняття запрошення, вихід з команди) використовують `player_uuid`, переданий клієнтом, для авторизації дій.
    *   **Ризик:** Будь-який зловмисник може надіслати запит з UUID іншого гравця і виконати від його імені критичні дії: перейменувати його команду, змусити вийти з неї або приєднатися до іншої. Це фундаментальна вразливість, що дозволяє повне захоплення управління командою будь-якого гравця.

### Модуль: Nestworldvelocity (Проксі-плагін)

1.  **Відсутність перевірки прав доступу до команд:**
    *   **Проблема:** У файлі `TeamCommand.java` метод `hasPermission` завжди повертає `true`.
    *   **Ризик:** Будь-який гравець на сервері може виконувати всі підкоманди `/team` (створення, перейменування, вихід), незалежно від наявності у нього прав.

2.  **Відсутність валідації вводу:**
    *   **Проблема:** Команди не перевіряють та не очищують вхідні дані, такі як назви команд.
    *   **Ризик:** Гравець може створити команду з назвою, що містить форматуючі коди або потенційно шкідливі символи, які можуть викликати проблеми з відображенням або обробкою даних в інших частинах системи.

### Модуль: FTB-Quests (Модифікований)

1.  **Довіра до даних, що надходять від клієнта:**
    *   **Проблема:** Файли `SubmitTaskMessage.java` та `ClaimRewardMessage.java` на сервері отримують від клієнта ID завдання (`taskId`) або винагороди (`rewardId`) і виконують перевірки безпеки (чи є гравець членом команди, чи знаходиться він на правильному острові). Однак, вони не перевіряють, чи належить саме це завдання/винагорода команді гравця.
    *   **Ризик:** Модифікований клієнт може надіслати ID завдання або винагороди, що належить іншій команді. Сервер успішно перевірить, що гравець знаходиться на *своєму* острові, а потім виконає завдання або видасть винагороду для *чужого* квесту. Це дозволяє гравцям несанкціоновано закривати квести та отримувати нагороди інших команд.

---

## ❌ Категорія 2: Порушення функціональної та логічної цілісності

### Модуль: API

1.  **Потенційні Race Conditions у фонових завданнях:**
    *   **Проблема:** Фонові завдання для операцій з LXD (`island_service.py`) створюють власні сесії бази даних. Основний потік запиту та фонове завдання працюють з БД в різний час, що може призвести до неузгодженого стану.
    *   **Ризик:** Система може спробувати виконати дію над островом (наприклад, запустити), який вже знаходиться в процесі запуску іншим запитом, що призведе до помилок.

2.  **Надмірно складна та нечітка логіка:**
    *   **Проблема:** Метод `start_island_instance` має складну вкладену логіку для обробки різних сценаріїв, що ускладнює її розуміння та підвищує ризик помилок. Коментар про необхідність додатково завантажувати дані вказує на потенційну проблему з неузгодженістю даних.
    *   **Ризик:** Некоректна робота при створенні або запуску островів для гравців у різних станах (в команді, без команди, зі старим островом).

3.  **Некоректна логіка видалення "соло" островів:**
    *   **Проблема:** Логіка в `handle_join_team` вважає будь-яку команду з одного учасника "соло" командою, яку можна безпечно видалити при приєднанні до іншої команди.
    *   **Ризик:** Якщо власник команди залишиться в ній один, а потім прийме запрошення в іншу команду, його острів буде безповоротно видалений, що не є очікуваною поведінкою.

### Модуль: Nestworldvelocity (Проксі-плагін)

1.  **Race Condition при перепідключенні гравця:**
    *   **Проблема:** Логіка скасування зупинки острова при перепідключенні та логіка призначення зупинки при виході працюють асинхронно і залежать від повільних API запитів.
    *   **Ризик:** Якщо гравець швидко перепідключиться, завдання на зупинку острова може бути призначене *після* того, як логіка скасування вже відпрацювала. В результаті острів зупиниться, незважаючи на те, що гравець знаходиться онлайн.

2.  **Ненадійна державна машина на основі polling:**
    *   **Проблема:** Метод `pollForRunningAndConnect` використовує вразливу логіку, засновану на порівнянні рядків статусу з API. Непередбачений статус або стан помилки може призвести до нескінченного циклу запитів без чіткого повідомлення для гравця.
    *   **Ризик:** Гравець може "застрягти" в лобі, не розуміючи, чому його острів не запускається. Система не може адекватно відновитися після помилок API.

3.  **Потенційний витік ресурсів при відключенні:**
    *   **Проблема:** Якщо гравець відключається під час запуску острова, плагін робить один-єдиний запит до API на зупинку острова. Немає механізму повторних спроб.
    *   **Ризик:** Якщо цей запит не вдасться, острів продовжить запускатися і буде споживати ресурси, залишаючись порожнім.

### Модуль: mods-server

1.  **Ненадійна логіка автоматичного заморожування:**
    *   **Проблема:** Механізм автозаморожування активується тільки якщо перший гравець приєднався протягом першої години після запуску сервера.
    *   **Ризик:** Якщо на сервер ніхто не зайде протягом години, функція автозаморожування буде вимкнена назавжди (до наступного перезапуску), що призведе до марної витрати ресурсів.

2.  **Відсутність "Heartbeat" сигналу:**
    *   **Проблема:** Мод повідомляє API про готовність лише один раз при старті.
    *   **Ризик:** Якщо сервер Minecraft "зависне" або впаде без спрацювання події зупинки, API продовжить вважати острів активним, направляючи гравців на непрацюючий сервер.

3.  **Необроблені збої API-запитів:**
    *   **Проблема:** Критично важливі сигнали (`/ready`, `/freeze`) надсилаються без логіки повторних спроб.
    *   **Ризик:** Тимчасова недоступність API під час старту або виходу останнього гравця призведе до втрати цих сигналів. Острів може ніколи не стати доступним для гравців або ніколи не заморозиться.

### Модуль: FTB-Quests (Модифікований)

1.  **Race Condition при доступі до даних команди:**
    *   **Проблема:** Основна логіка квестів (`BaseQuestFile`) виконує блокуючий синхронний запит до API (`getCachedTeamId`), якщо дані відсутні в кеші. Це відбувається паралельно з асинхронним завантаженням тих самих даних при старті сервера.
    *   **Ризик:** Це створює race condition, що може призвести до завантаження застарілих або неповних даних про команду, через що квести для гравця не будуть працювати коректно.

2.  **Небезпечне використання Singleton `IslandData.UNLOADED`:**
    *   **Проблема:** Якщо дані про команду недоступні, система використовує об'єкт-заглушку `IslandData.UNLOADED`.
    *   **Ризик:** Будь-які дії гравця (наприклад, виконання квестів) будуть мовчки ігноруватися, оскільки вони виконуються над об'єктом-заглушкою. Гравець не отримає повідомлення про проблему.

---

## 📈 Категорія 3: Перешкоди для дизайну та масштабованості

### Модуль: API

1.  **Неефективна архітектура WebSocket:**
    *   **Проблема:** Система використовує Redis Pub/Sub для трансляції повідомлень *усім* екземплярам API. Кожен екземпляр отримує кожне повідомлення і перевіряє, чи призначене воно для його локальних клієнтів.
    *   **Ризик:** Цей підхід ("fan-out") не масштабується. Зі збільшенням кількості екземплярів API та об'єму повідомлень, система буде витрачати значні ресурси на обробку зайвих даних.

2.  **Слабке логування:**
    *   **Проблема:** Логи не містять достатньо деталей для налагодження, особливо в WebSocket. Відсутні ID для кореляції, щоб простежити повний шлях запиту гравця.
    *   **Ризик:** Ускладнює діагностику проблем та безпечне впровадження нових функцій.

### Модуль: Nestworldvelocity (Проксі-плагін)

1.  **Неефективний механізм polling:**
    *   **Проблема:** Замість використання WebSocket для отримання подій, плагін постійно опитує API, щоб перевірити статус острова.
    *   **Ризик:** Створює значне непотрібне навантаження на API, яке буде зростати з кількістю гравців, що одночасно підключаються.

2.  **Відсутність централізованого управління станом:**
    *   **Проблема:** Стан підключення гравців зберігається в локальних змінних.
    *   **Ризик:** Архітектура не масштабується на кілька екземплярів проксі-сервера. Робота в такому режимі призведе до розсинхронізації стану та некоректної роботи.

### Модуль: mods-server

1.  **Блокуючі синхронні API-запити:**
    *   **Проблема:** Метод `refreshAndGetTeamId` виконує блокуючий мережевий запит на головному потоці сервера.
    *   **Ризик:** Якщо API працює повільно або недоступний, весь сервер Minecraft "зависне", очікуючи на відповідь. Це робить сервер надзвичайно вразливим до проблем з API.

2.  **Жорсткий зв'язок з FTB Quests:**
    *   **Проблема:** Мод напряму викликає методи з FTB Quests, створюючи жорстку залежність.
    *   **Ризик:** Неможливо оновити FTB Quests без ручної переробки інтеграції. Мод неможливо запустити або тестувати без FTB Quests.

### Модуль: FTB-Quests (Модифікований)

1.  **Жорсткий, хардкодовий зв'язок:**
    *   **Проблема:** Модифікації містять прямі, жорстко закодовані виклики до статичних класів з `mods-server`.
    *   **Ризик:** Це створює монолітну залежність. Оновлення будь-якого з компонентів стає вкрай складним і ризикованим. Модифікований FTB Quests неможливо використовувати поза цією екосистемою.

2.  **Відсутність чіткого шару абстракції:**
    *   **Проблема:** Логіка інтеграції розкидана по різних файлах, відсутній єдиний, чітко визначений "міст" між модами.
    *   **Ризик:** Код важко розуміти, підтримувати та розширювати, не вносячи нових помилок.

## III. Висновок

Екосистема "Dynamic SkyBlock LXD System" має серйозні архітектурні, логічні та безпекові проблеми, які необхідно вирішити. Критичні вразливості в API та відсутність перевірок прав доступу становлять безпосередню загрозу. Численні race conditions та блокуючі операції ставлять під загрозу стабільність та надійність системи. Жорсткий зв'язок між модулями робить систему крихкою та складною для подальшого розвитку.

Рекомендується негайно розпочати роботу з виправлення цих проблем, приділяючи першочергову увагу вразливостям безпеки та усуненню блокуючих операцій.
